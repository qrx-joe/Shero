[project]
name = "go2-door-check-demo"
version = "0.1.0"
description = "Door-check demo for a household OS dispatching a Unitree Go2 robot dog."
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
